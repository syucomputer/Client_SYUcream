import Subject from "../components/subject/Subject";

const SubjectPage = () => {
  return (
    <div
      style={{
        justifyContent: "center",
        alignItems: "center",
        margin: 0,
        padding: 0,
        position: "absolute",
      }}
    >
      <Subject />
    </div>
  );
};

export default SubjectPage;
