import Subject from "../components/Subject/Subject";

const SubjectPage = () => {
  return (
    <div
      style={{
        justifyContent: "center",
        alignItems: "center",
        margin: 0,
        padding: 0,
        position: "relative",
      }}
    >
      <Subject />
    </div>
  );
};

export default SubjectPage;
